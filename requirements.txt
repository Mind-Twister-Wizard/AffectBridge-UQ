numpy>=1.26,<2.1
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
soundfile>=0.12
librosa>=0.10.2
# Compatibility pins retained because this package was validated with the
# Kaggle legacy/API-token transition used by the original implementation.
kaggle>=1.6,<1.8
kagglehub==0.3.13
kagglesdk==0.1.23
transformers>=4.45,<5.0
accelerate>=0.34
jinja2>=3.1
tabulate>=0.9
pytest>=8.0
# PyTorch/torchaudio are installed separately by setup_and_run.* so an NVIDIA
# machine receives the CUDA wheel directly instead of downloading CPU Torch first.
